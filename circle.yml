general:
  branches:
    only:
      - master
machine:
  pre:
    - mkdir ~/.yarn_cache
  node:
    version: 6.8
dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
    - rm -rf node_modules
  cache_directories:
    - ~/.yarn_cache
  override:
    - yarn
